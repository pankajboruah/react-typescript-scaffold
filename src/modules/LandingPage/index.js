import React from 'react';
import Input from './components/Input';

const LandingPage = ({ name }) => {
	return (
		<div>
			Landing Page {name} <Input></Input>
		</div>
	);
};

export default LandingPage;
